# Scene 07 — Asset gallery (one render per family)

TODO narration (draft): Here is the whole catalogue at a glance — a block, a
disk, a ring, a cylinder, a rod, an incline, a pulley, a rope, and a hinge. Every
one is built from the same asset library with sensible defaults, and each carries
its own keypoints and free-body vocabulary. This is the toolbox: once the objects
are stable, later work just adds behaviour — state over time, events, and
overlays — on top of them.
