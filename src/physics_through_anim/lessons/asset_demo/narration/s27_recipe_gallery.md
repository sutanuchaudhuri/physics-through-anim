# Scene 27 — The recipe gallery

TODO narration (draft): Everything we have built comes together as recipes. A
recipe is a single call — kepler orbit, cylinder at a table edge, a chain pouring
over an edge, the Galperin collisions — and out comes a complete scene: the
assembly of bodies, the event timeline, the trajectories, ready to animate. Here
are a few, each drawn from just generic assets composed together, never a new
special-case class. A person writes the call; an assistant could build the very
same graph. That composability — the whole textbook from a handful of parts — is
the point of the library.
