# Scene 02 — M1.6 kinematics bindings

TODO narration (draft): One mechanism drives every rigid body. A block
translates, a rod rotates about a fixed hinge, then does both at once as general
plane motion. A point stays attached to a moving body, and a spring endpoint
follows the block as it slides. A wheel rolls without slipping, its spoke turning
at v equals omega R with no drift. An object follows a curved path, always
oriented along the tangent. Finally two linked rods move together through the
scene graph. Every pose here comes from the M1.6 transform and binding layer —
never from hand-tuned shifts and rotations.
