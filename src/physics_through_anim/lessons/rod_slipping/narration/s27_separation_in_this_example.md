# s27_separation_in_this_example

For our specific numbers, with a static friction coefficient of zero point three zero, the computed normal force never actually reaches zero before the foot runs out of table. In this example, it is the table's finite width that ends contact. The normal force criterion remains the correct general rule for lift-off; it simply is not the active constraint here.
