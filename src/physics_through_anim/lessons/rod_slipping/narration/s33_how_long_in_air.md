# s33_how_long_in_air

Finding how long the rod stays airborne requires solving for when either endpoint first reaches the floor, which we do with a numerical root-finder rather than an algebraic formula.
